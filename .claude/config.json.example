{
  "mcpServers": {
    "example-stdio-server": {
      "type": "stdio",
      "command": "node",
      "args": ["/path/to/mcp-server.js"],
      "env": {
        "API_KEY": "your-api-key-here",
        "DEBUG": "true"
      },
      "disabled": false
    },
    "example-http-server": {
      "type": "http",
      "url": "https://api.example.com/mcp",
      "headers": {
        "Authorization": "Bearer ${API_TOKEN}"
      },
      "disabled": false
    },
    "example-sse-server": {
      "type": "sse",
      "url": "https://api.example.com/mcp/sse",
      "headers": {
        "Authorization": "Bearer ${API_TOKEN}"
      },
      "disabled": false
    },
    "disabled-server": {
      "command": "python",
      "args": ["-m", "mcp_server"],
      "disabled": true
    }
  },
  "allowedTools": [
    "mcp__example-stdio-server__*",
    "mcp__example-http-server__*",
    "mcp__example-sse-server__query"
  ],
  "env": {
    "ENABLE_TOOL_SEARCH": "auto"
  },
  "plugins": {
    "example-plugin": {
      "enabled": true,
      "config": {
        "option1": "value1",
        "option2": true
      }
    }
  },
  "skills": {
    "example-skill": {
      "enabled": true,
      "config": {
        "customOption": "customValue"
      }
    }
  },
  "hooks": {
    "user-prompt-submit-hook": {
      "command": "bash",
      "args": ["-c", "echo 'User prompt submitted'"],
      "env": {
        "HOOK_TYPE": "user-prompt-submit"
      }
    },
    "tool-call-hook": {
      "command": "node",
      "args": ["/path/to/tool-call-logger.js"]
    }
  },
  "commands": {
    "deploy": {
      "command": "bash",
      "args": ["-c", "npm run deploy"],
      "description": "Deploy the application to production",
      "env": {
        "NODE_ENV": "production"
      }
    },
    "test": {
      "command": "npm",
      "args": ["test"],
      "description": "Run the test suite"
    }
  },
  "agents": {
    "code-reviewer": {
      "description": "Expert code review specialist for security and maintainability",
      "prompt": "You are a code review specialist. Focus on security vulnerabilities, performance issues, and code maintainability.",
      "tools": ["Read", "Grep", "Glob"],
      "model": "sonnet"
    },
    "explorer": {
      "description": "Fast codebase exploration specialist",
      "prompt": "You are a codebase exploration specialist. Quickly find and summarize relevant code.",
      "tools": ["Read", "Grep", "Glob", "find_symbol"],
      "model": "haiku"
    }
  },
  "tokenOptimization": {
    "enableExtendedContext": true,
    "maxBudgetUsd": 10.0,
    "maxThinkingTokens": 10000,
    "maxTurns": 50,
    "maxMessageHistory": 10000
  }
}
